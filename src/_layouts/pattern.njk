<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ title }}</title>
	<link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
</head>
<body class="pattern">
	<header class="activity {{ activity }}">
		<h1>{{ pattern }}</h1>
		<p class="definition">{{ definition }}</p>

	</header>
	<main class="{{ activity }}">

	{{ content | safe }}
	</main>
</body>
</html>
